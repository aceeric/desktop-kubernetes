#!/usr/bin/env bash

# generates id_ed25519 and id_ed25519.pub in the kickstart directory. These get included in the kickstart
# ISO that is generated by the 'gen-kickstart-iso' script. Since this is wired into the template VM, the
# private keys can be used to SSH into *any* VM cloned from the template. So the only time to re-gen this
# key pair is when creating a new template VM.

set -e

ssh-keygen -t ed25519 -N '' -f $DTKBASE/generated/kickstart/id_ed25519 <<<y
