#!/usr/bin/env bash
#
# Helm-installs the Metrics Server.
#

config=$1

CHART_VER=3.13.0

admin_kubeconfig=$(yq .cluster.admin-kubeconfig $config)

url=https://github.com/kubernetes-sigs/metrics-server/releases/download/metrics-server-helm-chart-$CHART_VER/metrics-server-$CHART_VER.tgz
tgz=$DTKBASE/binaries/$(basename $url)
xec download-obj --url=$url --dest=$tgz

helm upgrade --install metrics-server $tgz\
  --namespace metrics-server\
  --create-namespace\
  --kubeconfig $admin_kubeconfig\
  --wait
