# Images

If you place `.tar.` and `.tgz` files here, the installer will copy them to the host and load them into the containerd cache on each host to help minimize the impact of Docker rate limiting.
